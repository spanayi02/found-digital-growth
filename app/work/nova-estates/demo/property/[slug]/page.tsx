import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { NovaHeader } from "../../nova-header"; import { NovaFooter } from "../../nova-footer"; import { novaProperties } from "../../property-data"; import { PropertyExperience } from "./property-experience"; import "../../nova-demo.css";
export function generateStaticParams(){return novaProperties.map(p=>({slug:p.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=novaProperties.find(x=>x.slug===slug);return p?{title:`${p.title} | NOVA Estates`,description:p.summary}:{}}
export default async function PropertyPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const property=novaProperties.find(p=>p.slug===slug);if(!property)notFound();return <main className="nova-demo nova-inner"><NovaHeader solid/><PropertyExperience property={property}/><NovaFooter/></main>}
